# Publish the browser build to GitHub Pages.
#
# The web build is the same renderer as the macOS app with no server behind
# it: static JSON for every read, and Web Serial for the cable. See
# scripts/build/build-web.sh.
#
# Runs on a push to main that touches anything the build is made of, and can
# be triggered by hand. The generated tree is ~330 MB across ~42k files, so
# this is not a fast job -- most of the time is the artifact upload, not the
# build (the export itself is ~20s).
#
# No app is needed here: web_export.py falls back to data/chassis-config,
# committed for exactly this reason. Everything else is already a generated
# file in the repo.
name: pages

on:
  push:
    branches: [main]
    paths:
      - 'app/renderer/**'
      - 'data/**'
      - 'tools/export/**'
      - 'tools/sgbd/ecu_tree.py'
      - 'scripts/build/build-web.sh'
      - '.github/workflows/pages.yml'
  workflow_dispatch:

# Pages needs these; the default GITHUB_TOKEN is read-only for contents.
permissions:
  contents: read
  pages: write
  id-token: write

# One deploy at a time. Never cancel a running one -- a half-uploaded Pages
# artifact leaves the site serving a mix of two builds.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      # build_single.js runs the browser exporter in a VM context
      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Expand the committed data
        run: |
          set -euo pipefail
          # The committed data is gzipped. inpa-ir is read as .json by the tree
          # builder, so expand it in place first.
          find data/inpa-ir -name '*.json.gz' -print0 \
            | xargs -0 -r -P 4 -I{} gzip -dkf {}
          echo "expanded: $(find data/inpa-ir -name '*.json' | wc -l) files"

      - name: Assemble the per-car tree
        run: |
          set -euo pipefail
          # data/chassis/ is DERIVED and gitignored, so the runner has to build
          # it before anything can read it. It is what web_export reads now.
          python3 tools/export/build_ecu_tree.py
          test -d data/chassis/E46

      - name: Build the static site
        run: |
          set -euo pipefail
          # build-web.sh insists on a running app to resolve the chassis
          # config; here the exporter reads the committed cache instead, so
          # call the pieces directly rather than fighting that check.
          mkdir -p dist-web
          python3 tools/export/web_export.py --out dist-web
          cp -R app/renderer/. dist-web/
          # index.html already loads webshim.js -- both hosts need it.
          test -s dist-web/index.html
          grep -q webshim.js dist-web/index.html
          # A build that exported nothing must FAIL here, not deploy a shell
          # of a site: an earlier run shipped 63 files and looked green, and a
          # later one shipped archives that were present but hollow. Check
          # both: the archives exist, AND one holds real ECUs.
          n=$(find dist-web/api/chassis -name '*.chassis' | wc -l)
          echo "chassis archives: $n"
          test "$n" -ge 21
          python3 -c "import zipfile,sys; z=zipfile.ZipFile('dist-web/api/chassis/E90.chassis'); e=[n for n in z.namelist() if n.endswith('.ecu')]; print(f'E90 holds {len(e)} .ecu'); sys.exit(0 if len(e)>40 else 1)"

      - name: Wiring diagrams
        run: |
          set -euo pipefail
          # The .wiring archives are built from a WDS release, which is 4.7 GB
          # and not in this repo, so they cannot be generated here. They are
          # published to the wiring-images repo's releases by hand and pulled
          # in: 154 MB of diagrams and text, no photographs. Those come from a
          # CDN at runtime (see WIRING_IMG_CDN in screens/wiring.js) because a
          # Pages site may hold 1 GB and the images alone are 539 MB.
          #
          # Absent is not fatal: the site simply has no Wiring section, which
          # is what happened before this step existed.
          mkdir -p dist-web/data/wiring
          if gh release download wiring-latest \
               --repo dader34/BMacW-wiring-images \
               --pattern '*.wiring' --dir dist-web/data/wiring 2>/dev/null; then
            echo "wiring archives: $(ls dist-web/data/wiring/*.wiring | wc -l)"
            du -sh dist-web/data/wiring
          else
            echo "no wiring release found; publishing without the wiring section"
          fi
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Build the all-cars file
        run: |
          set -euo pipefail
          # THE ONE FILE THAT IS THE SAME FOR EVERYONE. Per-car copies are
          # built in the visitor's browser, which is cheap and covers every
          # combination -- but "all cars" is ~190 MB and identical for
          # every visitor, and assembling it peaks at several times that.
          # A phone does not survive it. Build it once, here.
          mkdir -p dist-web/get
          # Node sizes its heap from the machine's RAM, and a runner has
          # less than a laptop. The peak here is several times the 190 MB
          # result, so say the number rather than inherit one: an OOM would
          # abort the deploy, and this is the one step that can hit it.
          node --max-old-space-size=6144 \
            tools/export/build_single.js all-in-one dist-web/get --faults
          # A truncated or missing file must fail HERE, not be published as
          # a download that opens to nothing.
          test -s dist-web/get/bmweb-all.html
          python3 -c "
          import os, sys
          n = os.path.getsize('dist-web/get/bmweb-all.html') / 1048576
          print(f'all-cars file: {n:.0f} MB')
          sys.exit(0 if n > 100 else f'suspiciously small: {n:.0f} MB')"


      - name: Put the installer at the root
        run: |
          set -euo pipefail
          # THE PAGE BUILDS THE DOWNLOAD, so nothing is prebuilt here. The
          # site is the data tree plus install.html, and the exporter runs
          # in the visitor's browser against that tree -- any car, wiring or
          # not, fault text or not, without CI publishing every combination.
          #
          # index.html (the app) moves aside: an https page cannot open the
          # ws:// connection an adapter needs, so running the app from here
          # could never talk to a car. Downloading the file is the point.
          mv dist-web/index.html dist-web/app.html
          mv dist-web/install.html dist-web/index.html
          test -s dist-web/index.html
          grep -q offlineSingleFile dist-web/index.html

      - name: Report what is being published
        run: |
          echo "size:  $(du -sh dist-web | cut -f1)"
          echo "files: $(find dist-web -type f | wc -l)"

      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist-web

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
